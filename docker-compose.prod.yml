services:
  backend:
    image: ghcr.io/kon-drees/fake-news-detector-backend:latest
    restart: always
    env_file:
      - .env.prod
    expose:
      - "8000"

  frontend:
    image: ghcr.io/kon-drees/fake-news-detector-frontend:latest
    restart: always
    depends_on:
      - backend
    ports:
      - "80:80"